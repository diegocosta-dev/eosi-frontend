{% if title is defined and title %}
  <div class="lg:pt-[30px]">
    <h1 class="h1">
      {{ title }}
    </h1>

    {% if intro is defined and intro %}
      <h4 class="py-4 font-light text-gray-700 h4">
        {{ intro }}
      </h4>
    {% endif %}
  </div>
{% endif %}
